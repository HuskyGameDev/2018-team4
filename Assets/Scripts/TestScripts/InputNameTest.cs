using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputNameTest : MonoBehaviour {
	/*
	void Update () {
		if (Input.anyKeyDown) {
			//KeyCode key = Input.keyCode;
			Debug.Log("Pressed \"" + Input.inputString + "\"");
		}
	}*/
	
	/*
	public void OnGUI() {
		Event e = Event.current;
		//Debug.Log("Current event detected: " + e.type);
		if (e.isKey) {
			Debug.Log("Detected key code: " + e.keyCode);
		} if (e.isMouse) {
			Debug.Log("Detected key code: " + e.keyCode);
			Debug.Log("Detected mouse button: " + e.button);
		}
	}*/
}
